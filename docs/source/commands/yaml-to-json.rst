yaml-to-json
============

.. code-block:: bash

	usage: regolith yaml-to-json [-h] files [files ...]

	positional arguments:
	  files       file names to convert

	options:
	  -h, --help  show this help message and exit
