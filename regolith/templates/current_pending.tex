\documentclass[prl,tighten,amsmath,amssymb,floatfix]{revtex4-1}


% Packages
\usepackage{graphicx}
\usepackage{times}
\usepackage{booktabs,dcolumn}
\usepackage{siunitx}

\setlength{\parindent}{0pt}

% Document
\begin{document}
\bf Current and Pending Support
\bf {{-latex_safe(pi['name']).upper-}}
\vskip 14pt

{% for g in grants}
\vskip 12pt\noindent\bf Investigator: {{-latex_safe(pi['name'])-}}
\bf \ Other Agencies to which proposal submitted:
{{-latex_safe(g.get("other_funders_submitted", 'None'))-}}

\bf Project/Proposal Title:  {{-latex_safe(g['proposal_title'])-}}
{% if len(team) > 1}
\ (multi-PI grant.  {{-latex_safe(pi['initials'])-}}
amt = \${{-"{:,}".format(float(g['subaward_amount']))-}}
{% else %}
Total Award Amount \${{-"{:,}".format(float(g['amount']))-}}
{% endif %}

\bf Source of Support:
{{-g.get('funder', 'None')-}}

\bf Total Award Amount:
\${{-"{:,}".format(float(g.get('funds_awarded', 0)))-}}
\bf Total Award Period Covered:
{{-g.get('award_start_date')-}}
-
{{-g.get('award_end_date')-}}

\bf Location of Project: {{-g.get('institution')-}}

\bf Person Months per Year Committed to Project:
Academic:
{{-str(g.get('person_months_academic', 0))-}}
\bf \ Summer:
{{-str(g.get('person_months_summer', 0))-}}

\bf Describe Research Including Synergies and Delineation with
Respect to this Proposal/Award: {{-g.get('scope', 'False')-}}
{% end for %}

\end{document}
